classDiagram
    class Owner {
        +str name
        +str preferences
        +list pets
        +add_pet(pet)
        +find_pet(pet_name)
        +get_all_tasks()
    }

    class Pet {
        +str name
        +str species
        +int age
        +list tasks
        +add_task(task)
        +remove_task(task_title)
        +get_tasks()
        +get_pending_tasks()
        +find_task(task_title)
    }

    class Task {
        +str title
        +str task_type
        +int duration_minutes
        +int priority
        +str preferred_time
        +str frequency
        +bool completed
        +bool recurrence_generated
        +mark_complete()
        +is_recurring()
        +generate_next_occurrence()
    }

    class Scheduler {
        +get_task_records(owner)
        +sort_tasks_by_priority(tasks)
        +sort_tasks_by_time(tasks)
        +filter_tasks(owner, pet_name, completed, task_type)
        +generate_daily_plan(owner, available_minutes)
        +mark_task_complete(pet, task_title)
        +detect_conflicts(tasks)
        +explain_plan(plan)
    }

    Owner "1" --> "*" Pet : owns
    Pet "1" --> "*" Task : has
    Scheduler ..> Owner : reads owner data
    Scheduler ..> Pet : manages pet tasks
    Scheduler ..> Task : sorts and schedules